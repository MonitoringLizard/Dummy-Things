#include <stdio.h> 
/* "#include <>"" adds some features that come already installed with GCC, 
you just need to type this command */

int main(){ // this tells the program to run everything inside those {}
    printf("Hello world\n");/*well, i think its pretty easy here, 
    "printf()" is to print "Hello world in the output window"
    the "\n" may look strange, but its purpose is to make the selector in a terminal go into the next line
    */
    return 0; 
    // if the program doesnt explode, crash, die, get divorced or anything, it will return 0, telling everything is okay
}